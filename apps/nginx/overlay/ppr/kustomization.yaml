resources:
- ../../base

patches:
- path: patch/deployment.yaml

images:
- name: nginx
  newName: nginx
  newTag: 1.30.4

configMapGenerator:
- name: nginx-config
  files:
  - nginx.conf
- name: nginx-env-vars
  envs:
  - config.env
